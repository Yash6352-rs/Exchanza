expo-auth-session 
expo-crypto 
expo-web-browser
useSharedValue
useAnimatedStyle


Project: Exchanza – Skill & Goods Exchange Platform
// 0. Tech Stack Decision (Final)  (done)
    //Use THIS:
        Auth: Firebase Auth
        Database: Firestore (main backend)
        Chat: Firebase Realtime DB
        Offline: Realm (cache + pending actions)

    //Why this combo?  (done)
        Firebase → easy + scalable
        Realtime DB → best for chat
        Realm → offline-first (standout feature)


1. Core Features (FULL LIST)
    // Auth  (done)
        Signup / Login
        Profile setup

    //Posts (Offer / Request)  (done)
        Create post
        Edit / Delete
        Tags system
        Status (open / in-progress / completed)

    // Discovery Feed  (done)
        Card UI
        Filter by tags
        Search

    // Trade System  (done)
        Send proposal
        Accept / Reject
        Status lifecycle

    // Chat  (done)
        Real-time messaging
        Only after acceptance

    // Notifications  (done)
        Proposal received
        Accepted / Rejected
        Trade completed

    //Rating System  (done)
        Rate after trade
        Avg rating on profile

    //PDF Invoice (standout)  (done)
        Generate after trade
        View + Download

    //Profile  (done)
        Image upload
        Bio, skills
        Ratings

    //Settings  (done)
        Notification toggle
        Logout
        About app

    Admin Panel
        Manage users
        Manage posts
        Analytics

2. DATABASE SCHEMA (VERY IMPORTANT)
    // 👤 Users Collection  (done)
        users: {
            id,
            name,
            email,
            profileImage,
            bio,
            skills: ["react", "design"],
            rating: 4.5,
            totalReviews: 10,
            createdAt
        }
    // 📦 Posts Collection  (done)
        posts: {
        id,
        userId,
        type: "offer" | "request",
        title,
        description,
        tags: ["coding"],
        status: "open" | "in-progress" | "completed",
        acceptedTradeId: null,
        createdAt
        }
        
    // 🔄 Trades Collection  (done)
        trades: {
        id,
        postId,
        fromUserId,
        toUserId,
        offerText,
        requestText,
        status: "pending" | "accepted" | "rejected" | "completed",
        createdAt
        }

    // 💬

    //🔔 Notifications  (done)
        notifications: {
        id,
        userId,
        type: "proposal" | "accepted" | "rejected",
        message,
        isRead: false,
        createdAt
        }

    //⭐ Reviews  (done)
        reviews: {
        id,
        tradeId,
        reviewerId,
        reviewedUserId,
        rating,
        comment
        }

    //📄 Invoices  (done)
        invoices: {
        id,
        tradeId,
        fromUser,
        toUser,
        details,
        createdAt,
        pdfUrl
        }

3. REALM (Offline Use)

Use Realm for:
    pendingActions: {
    id,
    type: "CREATE_TRADE" | "SEND_MESSAGE",
    payload,
    status: "pending"
    }

Also cache:
posts
chats

// 4. AUTH DECISION

Use Firebase Auth  (done)
    Why:
    Easy integration with Firestore
    No extra complexity
    Enough for this project

Clerk is good but overkill here

// 5. CHAT SYSTEM
    Flow:
        Trade accepted
        Create chat room (tradeId)
        Messages stored in Realtime DB
        Features:
        Auto scroll (useRef)
        Timestamps
        Seen / delivered (optional)

// 6. NOTIFICATIONS SYSTEM

    Trigger events:
        New proposal
        Trade accepted
        Trade rejected
        Trade completed
    Simple approach:
        Store in Firestore
        Show badge count
    Advanced (optional):
        Firebase push notifications

//7. RATING SYSTEM  (done)
    Flow:
        After trade completed
        Both users rate each other
    Logic:
        Update avg rating
        newAvg = (oldAvg * total + newRating) / (total + 1)

//8. PDF INVOICE (IMPORTANT)  (done)
YES — You should implement this
    //What it includes:
        User A ↔ User B
        Trade details
        Date
        Signature (optional)
    //How:
        Use library:
            react-native-html-to-pdf
        Features:
            View inside app
            Download
            Share

9. ADMIN PANEL
(You can make simple version inside same app)

Admin Screens:
    📊 Dashboard
        Total users
        Total trades
        Active trades
    👥 User Management
        View users
        Ban user
    📦 Post Management
        Delete inappropriate posts
    🚨 Reports
        Flagged users/posts

// 10. SETTINGS + PROFILE
    ⚙️ Settings Screen
        Edit profile
        Notification toggle
        Privacy (optional)
        Logout
        About app

    //👤 Profile Screen  (done)
        Profile image (Firebase Storage)
        Bio
        Skills
        Ratings
        Past trades
        
// 11. MULTIPLE PROPOSALS (IMPORTANT LOGIC)
Scenario: B, C, D send proposal

When A accepts B:
    Do this:

    Update post:
        status: "in-progress"
        acceptedTradeId: "trade_B"
        Update other trades:
        status: "rejected"

    Send notification:
        “This trade is no longer available”

Don’t: Allow multiple active trades (for now)

12. NAVIGATION STRUCTURE

    // 👤 USER APP  (done)
    Bottom Tabs:
        🏠 Home (Feed)
        🔄 Trades
        ➕ Create Post
        🔔 Notifications
        👤 Profile

    // Stack Screens:  (done)
        Post Details
        Trade Proposal Modal
        Chat Screen
        Edit Profile
        Settings

    🛠 ADMIN APP (or hidden role)
        Drawer Navigation:
        Dashboard
        Users
        Posts
        Reports

FINAL APP FLOW
    User logs in
    Creates profile
    Browses feed
    Sends proposal
    Owner accepts
    Chat starts
    Trade completes
    Rating + invoice
    Post marked completed

--------------------------------------------------------------------------------------    

1. FOLDER STRUCTURE (PRO LEVEL)

👉 Use feature-based architecture (very important)

src/
│
├── app/                        # Expo Router / Navigation entry
│
├── components/                 # Reusable UI components
│   ├── common/
│   │   ├── AppButton.tsx
│   │   ├── AppInput.tsx
│   │   ├── Loader.tsx
│   │   ├── EmptyState.tsx
│   │
│   ├── cards/
│   │   ├── PostCard.tsx
│   │   ├── TradeCard.tsx
│   │
│   ├── modals/
│   │   ├── TradeProposalModal.tsx
│
│
├── features/                   # Feature-based modules 🔥
│   ├── auth/
│   │   ├── screens/
│   │   ├── hooks/
│   │   ├── services/
│
│   ├── posts/
│   │   ├── screens/
│   │   ├── components/
│   │   ├── services/
│   │   ├── hooks/
│
│   ├── trades/
│   │   ├── screens/
│   │   ├── services/
│   │   ├── hooks/
│
│   ├── chat/
│   │   ├── screens/
│   │   ├── services/
│
│   ├── notifications/
│   │   ├── screens/
│   │   ├── services/
│
│   ├── profile/
│   │   ├── screens/
│   │   ├── services/
│
│   ├── admin/
│   │   ├── screens/
│   │   ├── services/
│
│
├── services/                   # Global services
│   ├── firebase/
│   │   ├── config.ts
│   │   ├── auth.ts
│   │   ├── firestore.ts
│   │   ├── storage.ts
│   │   ├── realtime.ts
│
│   ├── realm/
│   │   ├── schema.ts
│   │   ├── database.ts
│   │   ├── syncService.ts
│
│
├── store/                      # Zustand / Redux
│   ├── useAuthStore.ts
│   ├── usePostStore.ts
│   ├── useTradeStore.ts
│
│
├── hooks/
│   ├── useNetworkStatus.ts
│
│
├── utils/
│   ├── constants.ts
│   ├── helpers.ts
│
│
└── assets/


2. UI SCREEN DESIGN BREAKDOWN

I’ll explain like you’re designing in Figma 👇  (done)

// 🔐 AUTH FLOW  (done)
    Login Screen
    Layout:
    App logo (top)
    Email input
    Password input
    Login button
    “Create account” text
    
// 🟩 Signup Screen  (done)
    Name
    Email
    Password
    Profile image upload
    Skills (chips/tags input)

// 🏠 HOME (DISCOVERY FEED)  (done)
    🧾Screen Layout
        Top:
            Search bar 🔍
            Filter button (tags)
        Main:
        List of Post Cards
            PostCard UI
            Each card:
                [User Avatar]  Name     ⭐ 4.5
                [Title]
                "I will teach React"
                [Tags]
                #coding #react

                [Type Badge]
                OFFER / REQUEST
                [Button]
                View Details →

// POST DETAILS SCREEN  (done)
    Sections:
    User info
    Full description
    Tags
    Button: “Propose Trade”

// TRADE PROPOSAL MODAL  (done)
    Fields:
    “What you offer”
    “What you want”
    Submit button

// TRADES SCREEN (TOP TABS)  (done)
    Tabs:
    1. Pending
        Requests sent / received
    2. Active
        Accepted trades
    3. Completed
        Finished trades

    // Trade Card:
        User A ↔ User B

        Status: ACTIVE

        [Open Chat]
        [Mark Complete]

// 💬 CHAT SCREEN    (done)
    Layout:
        Top: User name
        Middle: Messages list
        Bottom:
            Input field
            Send button
            Features:
            Auto scroll
            Time labels

// NOTIFICATIONS SCREEN    (done)
    List:
        🔔 New trade request
        🔔 Trade accepted
        🔔 Trade rejected
        
// CREATE POST SCREEN    (done)
    Fields:
        Type (Offer / Request toggle)
        Title
        Description
        Tags (multi-select)
        Submit button

//👤 PROFILE SCREEN  (done)
    Top:
        Profile image
        Name
        Rating ⭐
        Bio
    Sections:
        Skills
        Active posts
        Completed trades

// ⚙️ SETTINGS SCREEN    (done)
Options:
    Edit profile
    Notification toggle
    Logout
    About app

🛠 ADMIN UI
Drawer Navigation
    📊 Dashboard
        Total users
        Total trades
    👥 Users
        List
        Ban button
    📦 Posts
        Delete inappropriate posts

// 📱 FINAL NAVIGATION STRUCTURE
    // Bottom Tabs (User)  (done)
        🏠 Home
        🔄 Trades
        ➕ Create
        🔔 Notifications
        👤 Profile

    // Stack Screens  (done)
        Post Details
        Chat
        Edit Profile
        Settings

🧠 UX DETAILS (IMPORTANT)
    Add these to stand out:
    ✅ Loading states
    ActivityIndicator
    ✅ Empty states
    “No trades yet”
    ✅ Error states
    “Something went wrong”
    ✅ Smooth transitions
    Modal animations

Exchanza is a mobile platform where users exchange skills or services without money by 
creating “offer” and “request” posts. Users can browse a discovery feed, filter by interests, 
and send trade proposals to others. Once a proposal is accepted, a private chat opens to 
coordinate the exchange. After completing a trade, both users can rate each other and generate 
a downloadable PDF summary of the transaction. The app also supports notifications, user profiles, 
and offline functionality using local storage. An admin panel helps manage users, posts, and 
overall platform activity.



Remove-Item -Recurse -Force node_modules
Remove-Item -Recurse -Force android
Remove-Item -Force package-lock.json
npm install          
npx expo prebuild --clean
npx expo run:android


Notiifcation should be on
1 when user B sends Trade Proposal request to user A
so notify User A trade proposal request has comes
and when User A taps on Notiifcation it opens the Trade Request screen

2 when user A accepts the trade of user B
so notify User B
and when User B taps on Notiifcation it opens the Trade Active screen

3 when user A or B completed the trade 
so notify both User A & B
and when User A or B taps on Notiifcation it opens the Trade Completed screen

4 when user A rejcts the trade of user B
so notify User B
and when User B taps on Notiifcation it opens the Trade Rejected screen

5 when user A clicks download invoice
so notify User A thats invoice downloaded
and when User B taps on Notiifcation it opens the Trade Completed screen



